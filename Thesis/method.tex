\chapter{The Ansatz}
\label{ch:trial_wavefunction}

The three decisions of this thesis are implemented in the order they constrain each other.
The ansatz is fixed first, because it determines what energies the training procedure must
compute and what gradients the optimizer must handle.
This chapter implements the first decision.

Before any training can begin, one must decide what to train.
For a fermionic quantum system this is not a free choice: the wavefunction must satisfy
a set of hard constraints that cannot be softened, approximated, or deferred to the
optimizer.
It must be antisymmetric under particle exchange.
It must be equivariant under relabeling of identical particles.
It must encode the known short-range singular behavior imposed by the Coulomb interaction.
And it must do all of this while remaining differentiable, compact in parameter count,
and well-conditioned for gradient-based optimization.

These are not independent requirements, and satisfying one tends to complicate the others.
Antisymmetry via a Slater determinant fixes the nodal topology---but the nodes of the
noninteracting reference are not the true nodes, and improving them requires backflow
transformations that scale as $\mathcal{O}(N^2)$ in memory.
Equivariance via Deep Sets handles single-particle structure, but particle-only pooling
cannot represent the two-body correlations that are the dominant physics in the
strongly interacting regime.
Adding a pairwise branch introduces the information needed, but raw interparticle
distances have unbounded derivatives at coalescence, destabilizing the Laplacian.

This chapter works through these constraints in order, specifying at each step what the
constraint costs and what design choice resolves it.

\section{Preliminaries}
\label{sec:setup}

\paragraph{Hamiltonian and units.}
We study $N$ spin-$\tfrac12$ fermions in two spatial dimensions confined by a harmonic trap
and interacting via Coulomb repulsion,
\begin{equation}
\label{eq:dot-h}
H \;=\; \sum_{i=1}^N \Big( -\tfrac12 \nabla_{\mathbf r_i}^2 + \tfrac12 \,\omega^2 r_i^2 \Big)
\;+\; \sum_{1\le i<j\le N} \frac{1}{|\mathbf r_i-\mathbf r_j|},
\qquad (\hbar=m_e=e=1).
\end{equation}
Spin does not appear in $H$; eigenstates factorize into a spatial function with good $L_z$ and a
spin function with good $(S,S_z)$.

\paragraph{Coordinates and scaling.}
We collect positions as $R=(\mathbf r_1,\ldots,\mathbf r_N)\in\mathbb R^{2N}$ and use trap scaling
$\tilde{\mathbf r}_i=\sqrt{\omega}\,\mathbf r_i$, $\tilde{R}=\sqrt{\omega}\,R$. This aligns typical
length scales to $\mathcal O(1)$, improves conditioning of second derivatives, and makes the
noninteracting ground state $\omega$-independent. When useful, we also use pair distances
$r_{ij}=|\mathbf r_i-\mathbf r_j|$ and their scaled versions $\tilde r_{ij}=|\tilde{\mathbf r}_i-\tilde{\mathbf r}_j|$.

\paragraph{Sectors and focus.}
We primarily report \emph{closed-shell} cases (unique spin singlet at the noninteracting level).
Open shells introduce a degenerate manifold and additional choices for the reference; we keep this
to brief remarks only where needed.

\paragraph{Spin–orbitals and basis.}
One-particle spatial orbitals are Fock–Darwin functions $\{\phi_{nm}(\mathbf r)\}$ with
$E_{nm}=\omega(2n+|m|+1)$, $n\in\mathbb N_0$, $m\in\mathbb Z$. Spin–orbitals are
$\varepsilon_\alpha(x)=\phi_{nm}(\mathbf r)\chi_\sigma(s)$, $\sigma\in\{\uparrow,\downarrow\}$.

% ==========================================================

\section{Overview of the ansatz}
\label{sec:ansatz}
Our trial state combines exact antisymmetry, an analytic short-range cusp, and a compact
neural residual, while optionally evaluating the Slater determinant on backflowed coordinates:
\begin{align}
  \Psi_{\theta,\beta}(R)
  &= \mathrm{SD}\big(\tilde{R}'\big)\;
     \exp\!\Big(
       \sum_{i<j} u_{\sigma_i \sigma_j}(\tilde r_{ij})
       + W_\theta(\tilde{R})
     \Big),\label{eq:ansatz-overall} \\
  \tilde{R}' &= \tilde{R} + \Delta_\beta(\tilde{R}).\label{eq:bf-coords}
\end{align}
Here $\mathrm{SD}$ enforces antisymmetry; $u$ hard-wires the Kato cusp; $W_\theta$ (the NQS correlator) learns smooth
medium/long-range structure; and $\Delta_\beta$ (backflow) adjusts the nodes by smoothly warping the coordinates used
inside the determinants. We deliberately feed the NQS with $\tilde{R}$ (not $\tilde{R}'$) to avoid self-reinforcing feedback
and keep local-energy variance low.

\paragraph{Design principles.}
(i) \emph{Separate guaranteed physics from learned details:} antisymmetry and the cusp are hard-wired; the network learns the rest.
(ii) \emph{Condition the geometry:} work in trap units and use soft-core pair features so that first/second derivatives are bounded.
(iii) \emph{Keep the learned object small and smooth:} once the cusp is explicit, a compact NQS suffices across $(N,\omega)$.

\section{Slater reference}
\label{subsec:slater}
For closed shells we use a restricted product of spin-block determinants of the lowest $N/2$ harmonic oscillator orbitals (trap units):
\begin{equation}
  \mathrm{SD}(\tilde{R})
  =
  \det\!\big[\phi_a(\tilde{\mathbf r}_i)\big]_{i\in\uparrow,\;a\le N/2}\;
  \det\!\big[\phi_a(\tilde{\mathbf r}_j)\big]_{j\in\downarrow,\;a\le N/2}.
  \label{eq:slater}
\end{equation}
The correlator reweights amplitudes \emph{within} nodal pockets; without backflow, the nodes are those of~\eqref{eq:slater}.

\section{Neural correlator $W_\theta$}
\label{subsec:nqs}
The NQS aggregates \emph{particlewise} and \emph{pairwise} embeddings with permutation-invariant pooling and appends two global
statistics, all in $\tilde{R}$. A core safety requirement is that pair channels satisfy $d s_k/d\tilde r \to 0$ as $\tilde r\to 0$
so that $W_\theta$ never injects spurious $1/\tilde r$ terms into $\nabla\log\Psi$ or $\Delta\log\Psi$.

\subsection{Particle branch (DeepSets).}
This part of the network is an instance of the Deep Sets architecture~\cite{zaheer2018deepsets}.
A many-body wavefunction must be antisymmetric under particle exchange; the Slater determinant guarantees this, but the correlator $W_\theta$ multiplying it must be \emph{permutation-invariant} (symmetric) so that exchanging two particle labels leaves $\Psi$ with only the sign change from the determinant.
Deep Sets achieves this by construction: a shared network $\phi$ maps each particle's features to a latent embedding, and the embeddings are pooled symmetrically (here by mean) into a global representation that is insensitive to particle ordering.
A neural network $\phi$ creates a latent embedding for each particle individually. All embeddings are pooled together to form a global representation, which is used later in the readout. Formally,
\[
  \mathbf h_i^\phi=\phi(\tilde{\mathbf x}_i)\in\mathbb R^{d_L},\qquad
  \overline{\mathbf h}^{\phi}=N^{-1}\sum_i \mathbf h_i^\phi\in\mathbb R^{d_L}.
\]

\subsection{Pair branch (soft-core, even channels).}
The particle branch satisfies equivariance but cannot represent two-body correlations directly: each particle is processed independently, and the pooling discards relative geometry.
For a strongly interacting system---where the correlation energy is a substantial fraction of the total---this is not a limitation one can tune away.
We therefore add a dedicated pair branch that builds features from interparticle distances.
This is not an optional enhancement; it is the minimum required to capture the physics.
This network processes all pairs $(i,j), i\ne j$ individually and pools the resulting latent embeddings. 
The problem is that raw distances $\tilde r_{ij}$ have unbounded derivatives at coalescence, which destabilizes Laplacian evaluations. We therefore build \emph{safe} pair features with bounded first and second derivatives as $\tilde r\to 0$.
With $\tilde r^{\rm soft}=\sqrt{\tilde r^2+\varepsilon^2}$ ($\varepsilon\!\in[0.15,0.30]$ in trap units), we build six “safe” features
\begin{align}
  s_1 &= \log\!\big(1+(\tilde r^{\rm soft}/\varepsilon)^2\big),\quad
  s_2 = \tfrac{\tilde r^{2}}{\tilde r^{2}+\varepsilon^2},\quad
  s_3 = (\tilde r^{\rm soft}/\varepsilon)^{2}\exp[-(\tilde r^{\rm soft}/\varepsilon)^{2}], \notag\\
  \mathrm{rbf}_g &= \exp(-g\,s_1),\quad g\in\{0.25,1,4\},\qquad
  \mathbf u_{ij}=[s_1,s_2,s_3,\mathrm{rbf}_{0.25},\mathrm{rbf}_{1},\mathrm{rbf}_4]\in\mathbb R^{6}.
  \label{eq:safe-feats}
\end{align}
An MLP $\psi$ maps $\mathbf u_{ij}\mapsto\mathbf h^{\psi}_{ij}\in\mathbb R^{d_L}$. A short-range gate
\[
  \chi(\tilde r)=\frac{\tilde r^{2}}{\tilde r^{2}+r_g^{2}},\quad r_g\approx 0.3,\quad \chi(0)=\chi'(0)=0,
\]
suppresses learned responses \emph{at} coalescence, we use $\chi\,\mathbf h^\psi_{ij}$ downstream.
In practice the pair-input vector is augmented with direct coordinate
features $(\Delta x_{ij},\Delta y_{ij})$, one-body positions $(x_i,y_i)$,
and a spin indicator; these channels do not individually satisfy the
coalescence-safety condition.
Attribution analysis (§\ref{sec:attribution}) shows that the network
learns to suppress these raw-coordinate channels globally, relying
predominantly on the radial features across all confinement strengths:
the coalescence constraint is enforced by the architecture on the safe
features, and learned by the network for the remaining channels.

\subsection{Analytic short-range cusp}
\label{subsec:cusp}
Due to the necessity of the safe features in the pair branch, the NQS cannot represent the Kato cusp condition exactly. 
We therefore add an analytic cusp term to the exponent in~\eqref{eq:ansatz-overall}. 
We enforce the electron–electron cusp in trap units via
\begin{equation}
  u_{\sigma\sigma'}(\tilde r)
  =
  \gamma_{\sigma\sigma'}\,\tilde r\,e^{-\tilde r},
  \qquad
  \gamma_{\uparrow\downarrow}=\tfrac{1}{d-1},\quad
  \gamma_{\uparrow\uparrow}=\tfrac{1}{d+1}.
  \label{eq:cusp}
\end{equation}
Equivalently, in physical units $r$, $u(r)=\gamma_{\sigma\sigma'}\,(r/a_{\rm ho})\,\exp[-(r/a_{\rm ho})]$.
The linear coefficient yields the Kato slope at contact in general $d$; the exponential taper suppresses long-range interference, which means the NQS need not unlearn cusp structure at medium/long range.


\subsection{Global readout}
With contributions from the particle and pair branches, we form a global representation for the final readout. In addition to the pooled embeddings $\overline{\mathbf h}^\phi$ and $\overline{\mathbf h}^\psi$, we also include two global statistics that help the network gauge overall system size and density.
We average pairs by default or apply degree-normalized radial attention
\[
  w_{ij}=\exp[-(\tilde r_{ij}/r_c)^p],\quad
  \hat w_{ij}=w_{ij}\Big/\sum_{k\ne i}w_{ik},\quad
  (r_c,p)=(0.4,6),
\]
then form $g_i=\sum_{j\ne i}\hat w_{ij}\,\mathbf h^\psi_{ij}$ and $\overline{\mathbf h}^\psi=N^{-1}\sum_i g_i$.
Two system-level scalars in trap units,
\[
  \mathbf g(\tilde{R})=
  \Big[\tfrac{1}{Nd}\sum_i\|\tilde{\mathbf x}_i\|^2,\;
       \tfrac{2}{N(N-1)}\sum_{i<j}s_1(\tilde r_{ij})\Big]\in\mathbb R^2,
\]
are concatenated with $\overline{\mathbf h}^{\phi}$ and $\overline{\mathbf h}^{\psi}$ and mapped to a scalar by a tiny MLP $\rho$:
\begin{equation}
  W_\theta(\tilde{R})
  =
  \rho\!\Big(\overline{\mathbf h}^{\phi}\ \|\ \overline{\mathbf h}^{\psi}\ \|\ \mathbf g(\tilde{R})\Big).
  \label{eq:nqs-readout}
\end{equation}

\section{Backflow}
\label{subsec:backflow-method}

Backflow deforms the coordinates \emph{used only inside the Slater reference} so that the nodal surface can adapt to interparticle correlations:
\begin{equation}
  \tilde{R}' \;=\; \tilde{R} + \Delta_\beta(\tilde{R}) ,
  \qquad
  \Psi_{\theta,\beta}(R) \;=\; \mathrm{SD}(\tilde R')\;
  \exp\!\Big(\sum_{i<j} u_{\sigma_i\sigma_j}(\tilde r_{ij}) + W_\theta(\tilde R)\Big).
  \label{eq:bf-def}
\end{equation}
The correlator $W_\theta$ and cusp $u$ are evaluated on $\tilde R$ to avoid feedback loops in the local energy; only the determinant ``sees'' $\tilde R'$.
This choice cleanly separates (i) amplitude reweighting within nodal pockets (handled by $u+W_\theta$) from (ii) nodal \emph{geometry} (handled by backflow).

\paragraph{Design goals.}
(i) \emph{Stability near coalescence:} messages use mollified radii and short-range gates so that $\partial \Delta_\beta/\partial \tilde r$ remains bounded; \\
(ii) \emph{Symmetries:} permutation invariance by construction; near translation invariance by centering/projection; rotation equivariance via vector messages; \\
(iii) \emph{Close to identity at initialization:} zero-initialized last layer and a positive, learnable scale keep $\Delta_\beta\!\approx\!0$ at start.

\subsection{Message–passing construction}
We use a minimal two-stage message–passing update. For each ordered pair $(i,j)$ with $i\neq j$, form a message
\begin{align}
  \mathbf m_{ij}
  &= \phi_\beta\!\Big(
      \underbrace{\tilde{\mathbf x}_i}_{\text{node $i$}},\;
      \underbrace{\tilde{\mathbf x}_j}_{\text{neighbor $j$}},\;
      \underbrace{\mathbf r_{ij}}_{\tilde{\mathbf x}_i-\tilde{\mathbf x}_j},\;
      \underbrace{\tilde r_{ij}^{\rm soft}}_{\sqrt{\tilde r_{ij}^2+\varepsilon^2}},\;
      \underbrace{(\tilde r_{ij}^{\rm soft})^2}_{\text{scale}}
    \Big)\cdot w_{ij}^{\rm spin},
  \label{eq:bf-message}
\end{align}
where $\phi_\beta$ is a tiny MLP and $\varepsilon\!\ll\!1$ (trap units) mollifies the norm.\footnote{Each message network receives $(3d+2)$ features per pair: the trap-unit positions of both particles, the displacement vector $\mathbf{r}_{ij}$, and the two soft-core radial features $\tilde{r}_{ij}^{\rm soft}$ and $(\tilde{r}_{ij}^{\rm soft})^2$.}
Spin enters through a light‐weight mask $w_{ij}^{\rm spin}\in\{0,1\}$ (same‐spin only or all pairs).

Messages are aggregated into a node state
\begin{equation}
  \mathbf m_i \;=\; \mathrm{Agg}_{j\neq i}\,\mathbf m_{ij},\quad
  \mathrm{Agg}\in\{\mathrm{sum},\,\mathrm{mean},\,\mathrm{max}\},
\end{equation}
and a node/update network $\psi_\beta$ returns a displacement
\begin{equation}
  \delta\tilde{\mathbf x}_i \;=\; \psi_\beta\!\big(\tilde{\mathbf x}_i \ \| \ \mathbf m_i\big)
  \;\in\;\mathbb{R}^d.
\end{equation}
To keep displacements bounded and start near identity we apply
\begin{equation}
  \Delta_\beta(\tilde R)\;=\;\tanh\!\big(\delta\tilde X\big)\cdot s_\beta,
  \qquad s_\beta=\mathrm{softplus}(s_\beta^{\rm raw})>0,
  \label{eq:bf-bound-scale}
\end{equation}
with the last linear layer of $\psi_\beta$ zero‐initialized.
Empirically, this parameterization stabilizes SR/energy tails and protects Laplacians.

\subsection{Short-range behavior and gates}
As $\tilde r_{ij}\!\to\!0$, raw $1/\tilde r$ factors can make $\nabla\!\cdot\!\Delta_\beta$ spiky.
We therefore (i) mollify $\tilde r_{ij}$ as in~\eqref{eq:bf-message}, and optionally (ii) gate the message with
\begin{equation}
  \chi_{\rm bf}(\tilde r_{ij})=\frac{\tilde r_{ij}^2}{\tilde r_{ij}^2+r_g^2},
  \qquad \chi_{\rm bf}(0)=\chi'_{\rm bf}(0)=0,\quad r_g\approx 0.3,
  \label{eq:bf-gate}
\end{equation}
or with degree-normalized radial attention
\begin{equation}
  w_{ij}=\exp\!\big[-(\tilde r_{ij}/r_c)^p\big],\qquad
  \hat w_{ij}=\frac{w_{ij}}{\sum_{k\neq i} w_{ik}}.
\end{equation}
Either choice keeps the learned response smooth at coalescence and limits the BF contribution to the local energy variance.

\subsection{Symmetries and COM neutrality}
Permutation symmetry is automatic (pairwise messages and symmetric aggregation).
To avoid a spurious center-of-mass (COM) drift, we project the flow to zero mean:
\begin{equation}
  \Delta_\beta \;\leftarrow\; \Delta_\beta \;-\; \frac{1}{N}\sum_{i=1}^N \Delta_{\beta,i}.
  \label{eq:bf-com}
\end{equation}
With inputs expressed in trap units $\tilde R$ (already centered in practice) and with messages built from $\mathbf r_{ij}$, the update is effectively translation-invariant and rotation-equivariant.%
\footnote{Implementation note: subtracting the batchwise mean of $\Delta_\beta$ is a one-liner and removes the small residual COM drift seen in ablations.}

\subsection{Effect on the nodes and variational safety}
Because only $\mathrm{SD}$ is evaluated at $\tilde R'$, backflow changes the \emph{nodes} but leaves the correlator exponent on $\tilde R$. No Jacobian term is introduced (we are not performing a change of variables of the full wavefunction).
This ``Feynman--Cohen style'' design targets fixed-node error directly while keeping the amplitude model simple and smooth.

\subsection{Regularization, initialization, and cost}
We found the following defaults robust across $(N,\omega)$:
(i) $\varepsilon\in[10^{-6},10^{-4}]$ (trap units) for the soft norm;
(ii) bounded output $\tanh$ with a learnable scale $s_\beta$ initialized in $[0.02,0.10]$;
(iii) optional gate $\chi_{\rm bf}$ with $r_g\approx 0.3$ (trap units).
Zero-initializing the last layer of $\psi_\beta$ makes the ansatz start exactly at the Slater nodes and improves optimizer stability.
The compute/memory cost scales as $O\!\big(BN^2(d+H)\big)$ for batch size $B$ and hidden width $H$.

\subsection{Architectural summary}
The backflow network consists of two small MLPs: a message network $\phi_\beta$ that
computes pairwise messages from the $(3d+2)$-dimensional pair features, and a node-update
network $\psi_\beta$ that maps aggregated messages to the displacement $\Delta_\beta$.
Aggregation is selectable (sum, mean, or max); in all experiments reported here, sum
aggregation is used.
Spin handling is encoded by the binary mask $w_{ij}^{\rm spin}$, which can restrict
interactions to same-spin pairs or include all pairs.
The output nonlinearity in~\eqref{eq:bf-bound-scale} is tanh, with the bounding scale
$s_\beta$ parameterized as a softplus of a learned scalar to ensure positivity.
The center-of-mass neutrality condition~\eqref{eq:bf-com} is enforced by subtracting
the batch-mean predicted displacement before constructing $\tilde R'$.

\paragraph{Summary.}
Backflow provides a compact, symmetry-respecting way to move the Slater nodes toward the true correlated nodes. With mollified distances, optional short-range gates/attention, zero-mean projection, and bounded outputs, it integrates cleanly with the analytic cusp and smooth NQS correlator, reducing fixed-node error without inflating local-energy variance.

\section{The cell view: both networks as a unified message-passing system}
\label{sec:ctnn-cell}

The two neural components of the ansatz---the NQS correlator $W_\theta$
and the backflow displacement $\Delta_\beta$---share a common computational
motif: each particle maintains a state that is updated by pairwise messages
from its neighbors, then pooled symmetrically over all particles.
In the language of graph neural networks, the particles are nodes,
interparticle interactions are directed edges, and the two networks
apply the same message-passing update rule in separate feature spaces.
$W_\theta$ outputs a scalar (the log-amplitude correction); $\Delta_\beta$
outputs a vector (the coordinate displacement per particle).
Both are permutation-invariant by construction and share the same
short-range conditioning (soft-core mollification, optional radial gates).

This unified view has a concrete implication for expressivity.
A pairwise-only network can represent, at most, two-body correlations.
A message-passing network can, in principle, represent
\emph{three-body} and higher-order correlations: a message from particle
$j$ to particle $i$ already encodes information about $j$'s neighborhood,
and this accumulated state is available when $k$ subsequently interacts
with $i$.
Whether this expressivity advantage is actually exploited at different
confinement strengths is an empirical question; the results are reported
in §\ref{sec:mp-ablation}.

\paragraph{Three-body sensitivity at different confinement strengths.}
\label{subsec:ctnn-threebody}
Table~\ref{tab:threebody} reports the intra-bin variance ratio comparing
the CTNN cell output to the pairwise-only activation at $N{=}6$.
A ratio above $1.0$ indicates that the cell output encodes beyond-pairwise
information.

\begin{table}[h]
\centering
\caption{Three-body sensitivity ratio of the CTNN cell operation at
         $N{=}6$. Measured as the ratio of intra-bin variance of the
         cell activation to the variance attributable to the pairwise
         interaction alone.}
\label{tab:threebody}
\begin{tabular}{lc}
\toprule
Confinement $\omega$ & Three-body sensitivity ratio \\
\midrule
$1.0$   & $2.03\times$ \\
$0.1$   & $2.93\times$ \\
$0.001$ & $1.05\times$ \\
\bottomrule
\end{tabular}
\end{table}

At moderate-to-strong confinement ($\omega \ge 0.1$), the ratio is
substantially above unity: the cell operation draws on beyond-pairwise
information to represent the correlated fluid structure.
At deep Wigner confinement ($\omega{=}0.001$) the ratio collapses to
near unity.
This is not an architectural failure---it reflects the physical regime:
in the Wigner limit the correlation structure is dominated by the
classical lattice geometry, which is a pairwise phenomenon.
The cell's three-body capacity is present but becomes unnecessary once
the configuration space is effectively fixed by the lattice.

\chapter{Training the Wavefunction}
\label{ch:optimization}

Having specified what kind of wavefunction to parameterize, we turn to the
question of how to train it.
This is not, in general, the same as any other optimization problem.
The many-body Schrödinger equation imposes structure on the training landscape
that distinguishes it sharply from the generic deep-learning setting, and
that structure must be understood before the optimizer can be chosen,
the samples designed, or the gradient estimated.
This chapter develops the training pipeline around the three computational
layers that are specific to the problem: the pathology of Laplacian gradients
(Layer~1), the integrity of sampling without Markov chains (Layer~3),
and the regime-dependence of the optimizer (Layer~3).

\section{The Schrödinger equation as a training target}
\label{sec:schrodinger-target}

A physics-informed neural network solves a partial differential equation
by minimizing a residual loss over sampled collocation points.
For a generic elliptic PDE this is straightforward: the solution is smooth,
gradients of the loss with respect to network parameters are bounded,
and any reasonable optimizer makes progress.
The many-body Schrödinger equation is not generic in this sense.

\paragraph{Laplacian pathology near nodes and coalescence.}
The kinetic energy operator requires second derivatives of the wavefunction.
For a fermionic system, $\Psi$ changes sign at a nodal hypersurface,
and the Kato cusp condition forces a kink in $\log|\Psi|$ wherever two
electrons coincide.
At both locations, the Laplacian of an imperfect trial state diverges.
Direct backpropagation through the Laplacian at these configurations
produces pathological gradient signals: the training loss is largest
precisely where the gradient path is most unstable.

\paragraph{The catch-22 for backflow-augmented ansätze.}
Backflow transformations concentrate their learning signal near the nodal
surface, where small coordinate shifts produce large changes in the
determinant.
But the divergent Laplacian at those same configurations corrupts the
gradient through the backflow parameters.
The result is an irreducible conflict: the parameter directions that matter
most for accuracy are exactly those where direct backpropagation is most
unreliable.
Appendix~\ref{app:catch22} formalizes this catch-22 and provides numerical
evidence.
The resolution requires changing not what we optimize, but how we estimate
the gradient.

\section{Layer~1: gradient decoupling via REINFORCE}
\label{sec:colloc-training}

The variational energy satisfies
\begin{equation}
E(\theta)
= \mathbb{E}_{\mathbf{R}\sim|\Psi_\theta|^2}\!\bigl[E_L(\mathbf{R})\bigr],
\qquad
E_L(\mathbf{R})
= -\tfrac{1}{2}\bigl[
  \Delta\log|\Psi_\theta|
  + \|\nabla\log|\Psi_\theta|\|^2
  \bigr] + V(\mathbf{R}),
\label{eq:local-energy}
\end{equation}
and the zero-variance principle guarantees that $\mathrm{Var}[E_L] = 0$ if
and only if $\Psi_\theta$ is an exact eigenstate.
The energy gradient takes the REINFORCE form,
\begin{equation}
\nabla_\theta E(\theta)
= 2\,\EE_{\mathbf{R}\sim|\Psi_\theta|^2}\!\bigl[
  \bigl(E_L(\mathbf{R})-b\bigr)\,
  \nabla_\theta\log|\Psi_\theta(\mathbf{R})|\bigr],
\label{eq:reinforce-method}
\end{equation}
where $b$ is the batch mean of $E_L$, serving as a variance-reducing baseline.
The Laplacian $\Delta\log|\Psi_\theta|$ enters the reward $E_L$, but
$\nabla_\theta$ never differentiates through it.
Gradients act exclusively on the score function
$\nabla_\theta\log|\Psi_\theta|$, which is smooth near nodes and coalescence
points even when the Laplacian diverges.
This separation is not a numerical convenience; it is a structural choice
about what the gradient path should be, and it is the correct one.

Two mechanisms bound the reward signal against rare pathological
configurations.
Outlier local energies are clipped relative to a robust scale estimate
before entering the gradient.
An optional direct weak-form contribution was tested but set to zero in
all successful runs: pure REINFORCE, with no direct-gradient admixture,
consistently outperforms hybrid objectives because the cheaper energy
proxy it provides is also less accurate, and its gradient interferes with
the variational signal rather than reinforcing it.

Measured gradient norms confirm the structural advantage: REINFORCE
produces gradient norms $2.4\times$ lower than the finite-difference
collocation estimator on matched batches, providing more stable
optimization across seeds and at low $\omega$ where local-energy variance
is highest.
The finite-difference estimator can win a single-seed comparison at
$\omega{=}0.1$ where the local-energy surface is comparatively smooth,
but its higher-variance gradient degrades reliability across the
$(N,\omega)$ parameter space.

\section{Layer~3: sampling without Markov chains}
\label{sec:sampling}

Equation~\eqref{eq:reinforce-method} requires samples from $|\Psi_\theta|^2$.
Markov-chain Monte Carlo constructs such samples exactly, but at a cost:
autocorrelation, burn-in overhead, and mixing timescales that grow with
both $N$ and the complexity of $|\Psi|^2$.
The alternative is to draw samples from a tractable analytic proposal
and correct for the mismatch by importance resampling.

\subsection{Proposal distribution and importance weights}
\label{subsec:proposal}

We use a mixture of isotropic Gaussians centred at the trap minimum,
\begin{equation}
q(\mathbf{R})
= \frac{1}{K}\sum_{k=1}^{K}
  \mathcal{N}\!\bigl(\mathbf{R};\,0,\,\sigma_k^2(\omega)\,I_{Nd}\bigr),
\label{eq:proposal-method}
\end{equation}
with component widths $\sigma_k \propto 1/\!\sqrt{\omega}$ in physical
units so that the proposal tracks the harmonic-oscillator length scale
$\ell = 1/\!\sqrt{\omega}$ across all confinement strengths.
Three components---covering the core, bulk, and tails of the expected
density---provide coarse coverage of the relevant configuration space.

From this proposal, $m \times B$ candidates are drawn and each assigned
an unnormalized importance weight,
\begin{equation}
w_i = \frac{|\Psi_\theta(\mathbf{R}_i)|^2}{q(\mathbf{R}_i)},
\label{eq:is-weights}
\end{equation}
and the final training batch of $B$ points is obtained by resampling
according to the normalized weights.

\paragraph{A correctness requirement.}
The density $q(\mathbf{R}_i)$ in the denominator must be evaluated as
the \emph{mixture} density---a logsumexp over all $K$ component
log-densities.
Using the density of the generating component instead introduces a bias
of order $Nd$ nats in $\log q$, which amplifies to a factor of $e^{Nd}$
in the normalized weights.
At $N{=}6$, $d{=}2$, $\omega{=}0.01$ this is a corruption factor of
$e^{12} \approx 160{,}000$---invisible at tight confinement where the
components nearly agree, catastrophic at weak confinement and large $N$,
precisely the regimes of greatest physical interest.

\subsection{Effective sample size and the scaling wall}
\label{subsec:ess}

The quality of the importance-weighted gradient is measured by the
effective sample size,
\begin{equation}
\mathrm{ESS}
= \frac{\bigl(\sum_i w_i\bigr)^2}{\sum_i w_i^2}
\;\in\; [1, B].
\label{eq:ess-method}
\end{equation}
$\mathrm{ESS} = B$ when the weights are uniform; $\mathrm{ESS} \approx 1$
when a single sample dominates and the gradient has collapsed to a scaled
copy of the score function at one configuration---carrying essentially no
variational information.

ESS degrades with increasing $Nd$ and decreasing $\omega$ because the
wavefunction develops concentrated Wigner-molecule structure that an
isotropic Gaussian cannot track.
An adaptive oversampling mechanism draws $m \times B$ proposal candidates
and retains the $B$ with the largest importance weights; $m$ is adjusted
automatically when the realized ESS falls below a minimum floor.
This delays but cannot prevent ESS collapse: the number of candidates
required to maintain a fixed ESS grows exponentially with $Nd$, setting
a fundamental scaling limit on any MCMC-free approach.

\section{Layer~3: the optimizer regime boundary}
\label{sec:sr-stage}

The gradient from~\eqref{eq:reinforce-method} is a raw stochastic estimate.
How it is used to update $\theta$ is not neutral.

\paragraph{Stochastic reconfiguration.}
Writing $O_k(\mathbf{R}) = \partial_{\theta_k}\log\Psi_\theta(\mathbf{R})$,
the natural-gradient update on the variational manifold is
\begin{equation}
(S + \lambda I)\,\Delta\theta = -g,\qquad
S_{kl} = \mathrm{Cov}_\pi[O_k, O_l],\quad
g_k = 2\,\mathrm{Cov}_\pi[E_L, O_k].
\label{eq:sr}
\end{equation}
Stochastic reconfiguration (SR) solves this system---approximately, via
conjugate gradients with Tikhonov damping---to produce a geometry-aware
update that accounts for the curvature of the wavefunction manifold rather
than the raw Euclidean geometry of parameter space.
In well-conditioned regimes---moderate $\omega$, smaller $N$, high
ESS---SR outperforms first-order methods by a substantial margin.

\paragraph{The breakdown.}
SR requires a reliable estimate of $S$, accumulated over the
importance-weighted batch.
At low ESS the estimate is dominated by a handful of high-weight samples,
rendering it noisy and nearly rank-deficient.
Tikhonov damping stabilizes the linear solve, but at the cost of washing
out the curvature information that motivates SR in the first place.
Below $\omega \lesssim 0.01$ or beyond $N \approx 12$, Adam's
per-parameter momentum-based schedule is more robust precisely because it
does not attempt to model curvature globally.
The handoff from SR to Adam is not a tuning choice; it is the correct
response to the geometry of the sampling problem.

A clean illustration of why the handoff matters: applying overdamped
Langevin dynamics to move proposal samples toward $|\Psi|^2$ before
importance resampling was designed to improve ESS without running a full
chain.
It produced a $+153\%$ energy error at $N{=}20$, $\omega{=}0.1$,
versus $+5.7\%$ without refinement.
The Langevin steps produce a non-equilibrium sample distribution that
invalidates the importance weights, and the resulting gradient bias
compounds with the wavefunction deformation it induces.
Any strategy that modifies samples before importance resampling must
account for the changed effective proposal, or the weights are simply wrong.

\section{The two-stage pipeline}
\label{sec:opt-overview}

The REINFORCE pipeline is the primary training route throughout this
thesis for systems where MCMC is avoided.
A complementary pipeline uses Markov-chain sampling throughout, followed
by a natural-gradient refinement stage.
Both share the same ansatz and the same local-energy evaluation.

\paragraph{Stage I: residual pretraining.}
For a batch $\{\tilde{\mathbf{R}}_b\}$ drawn from the current proposal,
we minimize
\begin{equation}
\mathcal{L}_{\rm res}(\theta)
= \frac{1}{B}\sum_{b=1}^B
  \bigl(E_L(\tilde{\mathbf{R}}_b) - E_{\rm eff}\bigr)^2,
\qquad
E_{\rm eff} = \alpha\,E_{\rm DMC} + (1-\alpha)\,\mu,
\label{eq:residual}
\end{equation}
where $\mu$ is the batch mean of $E_L$ and $\alpha$ is annealed from~$0$
to~$1$ over the pretraining horizon.
At $\alpha = 0$, minimizing $\mathcal{L}_{\rm res}$ is exactly variance
minimization: the network learns to produce smooth, low-variance amplitudes
inside nodal pockets before any external target is imposed.
As $\alpha$ rises, the batch mean is replaced gradually by the DMC
reference, steering the energy without abrupt objective switches.

\paragraph{Stage II: stochastic reconfiguration.}
After pretraining, the SR update~\eqref{eq:sr} is applied using MCMC
samples from $|\Psi_\theta|^2$.
Centering and whitening of $O$ improve conditioning; a trust-region bound
limits the parameter step; Tikhonov damping stabilizes rank-deficient
directions.
This stage exploits the zero-variance principle: once the network
represents smooth, low-variance amplitudes, the geometry of the
variational manifold is well-defined and SR makes rapid, curvature-guided
progress.

\paragraph{Cascade warm-starting for low confinement.}
At small $\omega$, cold-start training from random parameters does not
converge for $N \geq 6$.
The correlation geometry learned at $\omega = 0.1$ transfers effectively
to $\omega = 0.01$, and from there to $\omega = 0.001$; each stage
inherits the full checkpoint from its predecessor.
The cascade sequence $\omega: 1.0 \to 0.5 \to 0.1 \to 0.01 \to 0.001$
converts an otherwise intractable initialization into a sequence of
tractable fine-tuning problems.

\section{Units and scaling}
\label{sec:units-scaling}

All coordinates are expressed in trap units $\tilde{\mathbf{r}} = \sqrt{\omega}\,\mathbf{r}$
so that the harmonic confinement energy takes the universal form
$\tfrac{1}{2}\sum_i|\tilde{\mathbf{r}}_i|^2$ and length scales are
comparable across all $\omega$ studied.
The local energy in trap units reads~\eqref{eq:local-energy} with the
Coulomb repulsion $\sum_{i<j}\sqrt{\omega}/\tilde{r}_{ij}$.
The proposal widths and shell radii are set in physical units and rescaled
by $1/\sqrt{\omega}$, ensuring that the components of the Gaussian mixture
cover comparable dimensionless regions across all confinement strengths.

\chapter{Diagnostics}
\label{ch:analysis}

The physical motivation for each diagnostic quantity --- what it measures, why it distinguishes
Fermi-liquid from Wigner-molecule states, and which observables are theoretically expected to
change across the crossover --- is established in §\ref{th:analysis-struct}.
This chapter describes how each quantity is computed on fresh $|\Psi_\theta|^2$ samples.
The diagnostics serve two purposes: structural validation, confirming that
the trained wavefunction captures the correct physics rather than merely
a low energy; and representation analysis, probing what the network has
actually learned.
All analyses operate on thinned samples that are held out from training.
\section{Structural diagnostics and Wigner markers}
\label{sec:analysis-struct}

Unless stated otherwise, curves are accumulated in \emph{trap units} 
$\mathbf{y}=\sqrt{\omega}\,\mathbf{x}$ to compare shapes across $\omega$, 
while all headline distances are reported in \emph{Bohr} (physical units $\mathbf{x}$).
Production chains are thinned; we split samples into halves for stability checks 
(acceptance window, JSD between halves, and reproducibility under restarts).
Using trap units to compare shapes across confinements is standard in parabolic dots; 
see the overview in~\cite{manninen2007metalclustersquantumdots}.

\subsection{Pair distribution and radial statistics}
\label{subsec:gr-pr}

We estimate the pair distribution function $g(r)$ via a shell‐area corrected histogram in trap units,
\begin{equation}
g(r_\alpha)\;=\;\frac{A_{\mathrm{eff}}}{N(N-1)}\;
\frac{\big\langle\sum_{i<j}\mathbf{1}\{r_{ij}\!\in[r_\alpha^-,r_\alpha^+)\}\big\rangle}
{2\pi r_\alpha\,\Delta r_\alpha},
\qquad r_{ij}=\|\mathbf{y}_i-\mathbf{y}_j\|_2,
\end{equation}
with $A_{\mathrm{eff}}=\pi R_{0.95}^2$ and $R_{0.95}$ the 95\% quantile of single‐particle radii in trap units.
This $g(r)$-based structural analysis follows established practice in mesoscopic 2D electron systems
and quantum dots~\cite{Filinov_2001,Egger_1999,manninen2007metalclustersquantumdots}.
We also form the normalized \emph{radial probability}
\(
P(r)\propto r^{d-1}g(r)
\)
and report the mode $r_{\mathrm{mode}}$, mean $\bar r$, standard deviation $\sigma_r$, and FWHM (all in Bohr).
A compact width–to–scale marker is
\begin{equation}
\gamma \;=\; \sigma_r / r_{\mathrm{mode}},
\label{eq:gamma}
\end{equation}
which decreases as correlations strengthen (narrower distribution around a larger typical radius),
consistent with the liquid$\to$Wigner crossover picture~\cite{Egger_1999,Filinov_2001}.

\subsection{Automatic shell detection and topologies}
\label{subsec:shells}

For each frame we sort the single‐particle radii $s_1\le\cdots\le s_N$ (trap units), compute gaps
$g_k=s_{k+1}-s_k$, and flag the frame as \emph{two‐shell} if the largest gap exceeds a robust threshold:
\begin{equation}
\max_k g_k \;\ge\; \tau\,\mathrm{median}(g_k),
\qquad \tau\in[2.5,3.5]\ \text{(default 3.0)}.
\end{equation}
The cut radius is the midpoint of the maximal gap; the inner multiplicity $n_{\mathrm{in}}$ defines the topology 
$(n_{\mathrm{in}}, N-n_{\mathrm{in}})$.
Shelling and discrete ring occupancies are well known in parabolic Coulomb clusters
and quantum dots~\cite{schweigert1994spectralpropertieschargedparticles,Kong_2002,manninen2007metalclustersquantumdots};
our detector provides a reproducible, data-driven assignment in that spirit.
We report:
(i) the fraction of two‐shell frames,
(ii) the histogram over $n_{\mathrm{in}}$ among two‐shell frames,
and (iii) for selected topologies (e.g.\ $(1,5)$ or $(3,9)$) the fraction among \emph{all} frames.

\paragraph{Shell‐resolved reconstruction of $g(r)$.}
On two‐shell frames we decompose pairs into II, IO, and OO and accumulate histograms
$g_{\mathrm{II}},g_{\mathrm{IO}},g_{\mathrm{OO}}$. With empirical weights
$w_{\mathrm{II}},w_{\mathrm{IO}},w_{\mathrm{OO}}$ (combinatorial counts) we reconstruct
\(\tilde g(r)=w_{\mathrm{II}}g_{\mathrm{II}}+w_{\mathrm{IO}}g_{\mathrm{IO}}+w_{\mathrm{OO}}g_{\mathrm{OO}}\)
and report $\cos\angle(g,\tilde g)$.
Values $\approx 1$ indicate that the observed $g(r)$ shape is explained by shell geometry rather than sampling artifacts,
in line with shell-based descriptions in~\cite{schweigert1994spectralpropertieschargedparticles,Kong_2002,manninen2007metalclustersquantumdots}.

\subsection{Orientational order and “ring stiffness”}
\label{subsec:orient}

On each detected ring (inner or outer) we center the mean angle to remove global rotations and define 
angles $\{\phi_k\}_{k=1}^n$.
Bond‐orientational order is
\begin{equation}
\Phi_m \;=\; \frac{1}{n}\sum_{k=1}^n e^{\,\mathrm{i} m \phi_k},
\qquad m=n,
\end{equation}
so that $|\Phi_m|\in[0,1]$ measures $n$‐fold orientational order (e.g.\ $m{=}5$ for $(1,5)$, $m{=}9$ for the outer ring of $(3,9)$),
analogous to standard bond–orientational order parameters used in 2D Coulomb/Yukawa crystals~\cite{Mazars_2008}.
As a complementary, dimensionless \emph{angular Lindemann} number we use
\begin{equation}
\lambda_\phi \;=\; \frac{\mathrm{std}(\Delta\phi)}{\mathbb{E}[\Delta\phi]},
\qquad 
\Delta\phi\;=\;\text{nearest‐neighbour angular spacing on the ring}.
\end{equation}
Smaller $\lambda_\phi$ indicates a stiffer (more Wigner‐like) ring.
This is a Lindemann–type orientational metric tailored to finite rings, motivated by
the use of Lindemann criteria to diagnose intershell rotational (angular) melting in mesoscopic
Coulomb clusters and dots~\cite{schweigert1994spectralpropertieschargedparticles,Filinov_2001}.

\subsection{Scaling check for $N=2$}
\label{subsec:n2-scaling}

To connect with the classical strong‐coupling limit we fit 
\begin{equation}
r_{\mathrm{mode}}(\omega) \;=\; C\,\omega^{\alpha}
\end{equation}
by linear regression in $\big(\log\omega,\log r_{\mathrm{mode}}\big)$ and compare the slope with the classical reference 
$\alpha_{\mathrm{cl}}=-2/3$ (two charges in a harmonic trap at strong coupling; see, e.g., the reviews in~\cite{manninen2007metalclustersquantumdots}).
Approach toward $\alpha_{\mathrm{cl}}$ as $\omega\downarrow$ is one of our Wigner crossover indicators,
consistent with the crossover phenomenology reported in~\cite{Egger_1999,Filinov_2001}.

\subsection{Reporting and stability checks}
\label{subsec:analysis-sanity}

For all scalars and histograms we report standard errors calculated over thinned blocks or frames.
We verify: 
(i) acceptance in the target window, 
(ii) Jensen–Shannon divergence and $\ell_2$ distance between halves of production (stationarity),
(iii) robustness of topology fractions and $|\Phi_m|$ under $\tau\in[2.5,3.5]$ and mild pre‐smoothing of radii, and
(iv) reproducibility of $g(r)$ and $P(r)$ under independent sampler restarts.
All diagnostics operate directly on saved bundles 
$\{\mathbf{X}_{\mathrm{Bohr}},\,r,\,g(r)\}$.

\section{Representation analysis}
\label{sec:repr-methods}

We probe what the networks learn by evaluating fixed, trained models on fresh
$|\Psi_\theta|^2$ samples. Unless stated otherwise, features are computed in \emph{trap units}
$\mathbf{y}=\sqrt{\omega}\,\mathbf{x}$ to match the correlator’s internal scaling.
All estimates use thinned MCMC frames, split into two equal blocks $A$ and $B$;
PCA fits on $A$ and reports on $B$ (and vice versa) to avoid circularity. 
We use \texttt{float64} throughout.

\paragraph{Feature taps.}
Let $z_\theta\in\mathbb{R}^D$ denote the concatenated correlator representation
\[
z_\theta=\big[\overline{\phi}\;\big|\;\overline{\psi}\;\big|\;\mathbf{g}\big],
\]
where $\overline{\phi}$ are per-particle summaries, $\overline{\psi}$ are pairwise summaries,
and $\mathbf{g}$ are global/extras. The residual head is linear:
\begin{equation}
f_{\rm base}(z)=\rho_\theta(z)=w^\top z+b,
\qquad
f=f_{\rm base}+f_{\rm cusp}.
\end{equation}
Backflow returns a displacement field $\Delta x\in\mathbb{R}^{N\times d}$ applied before
the Slater and correlator pipelines.

\subsection{Preprocessing and PCA}
\label{subsec:repr-prep}
Given a sample matrix $Z\in\mathbb{R}^{K\times D}$ (rows are frames), we standardize each
column using means and standard deviations estimated on block $A$:
\[
\tilde Z=(Z-\mu_A)\odot \sigma_A^{-1}.
\]
We perform PCA on $\tilde Z_A$ (SVD of the covariance) to obtain orthonormal loadings
$U=[u_1,\dots,u_D]$. Scores on $B$ are $S_B=\tilde Z_B U$. Unless noted, 
all quantities reported below are computed on the held-out block.

\subsection{Entropy effective rank}
\label{subsec:repr-erank}
Let $\{\lambda_i\}_{i=1}^D$ be the eigenvalues of $\mathrm{Cov}(\tilde Z)$
(on the held-out block). With $p_i=\lambda_i/\sum_j\lambda_j$, the
\emph{entropy effective rank} is
\[
r_{\rm eff}(Z)\;=\;\exp\!\Big(-\sum_{i=1}^D p_i\log p_i\Big),
\]
which equals $1$ for perfectly one-dimensional representations and increases
with spectral spread.

\subsection{PC ablations of the head}
\label{subsec:repr-pc-ablate}
To test how many latent axes the head truly uses, we project $\tilde Z$ onto the top-$k$ PCs:
\[
\Pi_k=\sum_{i=1}^k u_i u_i^\top,\qquad z^{(k)}=\Pi_k \tilde z,
\]
and re-evaluate the trained linear head \emph{without refitting}:
$\hat f^{(k)}=\rho_\theta(z^{(k)})$.
We report a \emph{normalized} error on block $B$,
\[
\mathrm{rel\text{-}MAE}(k)\;=\;
\frac{\mathbb{E}_B\,|\,\hat f^{(k)}-f_{\rm base}\,|}
     {\mathrm{std}_B(f_{\rm base})},
\]
together with the learning curve in $k$.

\subsection{PC1 block power decomposition}
\label{subsec:repr-pc1-power}
To attribute the leading axis to branches, we partition $u_1$ conformably with
$z=[\overline{\phi}\,|\,\overline{\psi}\,|\,\mathbf{g}]$ and report
\[
\text{power}(\overline{\phi})=\frac{\|u_{1,\overline{\phi}}\|_2^2}{\|u_1\|_2^2},
\quad
\text{power}(\overline{\psi})=\frac{\|u_{1,\overline{\psi}}\|_2^2}{\|u_1\|_2^2},
\quad
\text{power}(\mathbf{g})=\frac{\|u_{1,\mathbf{g}}\|_2^2}{\|u_1\|_2^2}.
\]
Because PCA is performed on standardized features, these shares are comparable across blocks.

\paragraph{Linear probes and near-field diagnostics.}
As supplementary analyses, we regress physical observables
$\{\bar{r},\,\mathrm{Var}(r),\,\Pr(r < r_0)\}$ on the first $k$ PC scores
using ridge regression, reporting $R^2$ on the held-out block.
We also measure near-field stiffness of the residual head by computing
the fraction of gradient norm $\|\nabla f_{\rm base}\|^2$ concentrated
in the lowest $q\%$ of frames by minimum pair distance: values near unity
confirm that the analytic cusp absorbs the short-range structure, leaving
the network free to capture global correlations.

\subsection{Backflow geometry and energy-locality shares}
\label{subsec:repr-bf}

\paragraph{Field spectrum.}
We vectorize the backflow displacement per frame,
$\delta=\mathrm{vec}(\Delta x)\in\mathbb{R}^{Nd}$, standardize across frames,
and compute $r_{\rm eff}(\Delta x)$ via Sec.~\ref{subsec:repr-erank}.

\paragraph{Where backflow acts.}
On the same thinned frames we evaluate local energies with and without backflow,
$E_{\rm loc}^{\rm BF}$ and $E_{\rm loc}^{\rm noBF}$, using the \emph{same} coordinates.
We form the signed correction $\Delta E_{\rm loc}=E_{\rm loc}^{\rm BF}-E_{\rm loc}^{\rm noBF}$ and report
near-field shares at quantile $q$ of $r_{\min}$:
\[
\mathrm{NF}\text{-}\Delta E(q)\;=\;
\frac{\mathbb{E}\big[|\Delta E_{\rm loc}|\,\mathbf{1}\{r_{\min}\le Q_q\}\big]}
     {\mathbb{E}\big[|\Delta E_{\rm loc}|\big]}.
\]
(Using absolute values isolates magnitude; signed averages are also available.)

\subsection{Alignment of correlator manifolds (noBF vs BF)}
\label{subsec:repr-alignment}
We collect two standardized feature matrices $\tilde Z^{\rm noBF}$ and $\tilde Z^{\rm BF}$
on identical frames (each standardized by its own $\mu,\sigma$), compute their PC1 loadings
$u_1^{\rm noBF}$ and $u_1^{\rm BF}$, and report the cosine similarity
\[
\cos\big(u_1^{\rm noBF},u_1^{\rm BF}\big)\;=\;
\frac{\langle u_1^{\rm noBF},\,u_1^{\rm BF}\rangle}
     {\|u_1^{\rm noBF}\|_2\,\|u_1^{\rm BF}\|_2}.
\]
Cosines near $1$ indicate that backflow acts as a structured reweighting along the same dominant axis,
rather than rotating the correlator’s manifold.

\subsection{Uncertainty and stability}
\label{subsec:repr-uncertainty}
For scalars we report standard errors over blocks.
For PCA-based quantities and cosines we use a block bootstrap (resampling blocks with replacement)
to obtain 68\%/95\% CIs. Stability checks include:
(i) swapping train/held-out blocks for PCA,
(ii) varying the near-field quantiles $q\in\{1,5,10\}\%$,
(iii) re-running analyses on independent sampler restarts.

\paragraph{Reproducibility package.}
All diagnostics consume the saved bundles 
$\{\mathbf{X}_{\rm Bohr},\,r,\,g(r)\}$ and the model snapshot,
and emit JSON/CSV tables with $r_{\rm eff}$, rel-MAE$(k)$ curves, block-power shares,
$R^2$ probes, near-field shares, $r_{\rm eff}(\Delta x)$, and noBF/BF cosines.
As in Sec.~\ref{sec:analysis-struct}, we log JSD/$\ell_2$ splits to confirm stationarity.
